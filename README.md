# Day Log

Static single-file habit tracker (GitHub Pages host). Contains no personal data — habits and logs live in a private repo, accessed from the app via the GitHub API with a device-local token. Source of truth: `personal-improvement/tools/habit-tracker/`.
